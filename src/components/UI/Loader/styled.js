// @flow
import styled from 'styled-components';

const Loader = styled.div`
  color: #FF0000;
  font-size: 20px;
  font-weight: bold;
  margin-top: -50px;
  text-align: center;

  img {
    width: 320px;
  }
`;

export default Loader;
